public class OOPS_2 {

    public static void main(String[] args) {
    // Packages : They are containers for classes.
    // Static  : The variables and methods which are independent of the objects are called static.
    //         : Whenever accessing the static methods or variables, access them with Class name, not with Obj name..,
    //           Both will work, but the convention is that we use Class name.
    // Static method : In a static function, you can't use any other function which is not static
    //               : You can use a static function inside a not static function
    //               : The classes which are not under other classes cannot be static.
    // Singleton Class : A class of which only one obj can be created
    }
}
